# TODO — Circle Nurture

*(Document 3 — the open-items tracker. Conductor-side pre-build items and build-side work. Nothing here stays open silently; each row is either done, in progress, or blocked with a written reason.)*

## Conductor pre-build (before the user walks away)
- [ ] **Self-audit the apparatus (Law 30)** — a DIFFERENT agent audits all sixteen documents: ten categories, an adversarial break-it pass, a by-command census, and GL-001…GL-008 validation. Fix until 8.5+. *(Task 8.)*
- [ ] **Create the GitHub repo** — `circle-nurture` on `layakelley`, branch `main`; token already smoke-tested. *(Task 9.)*
- [x] **Write the launch command + multi-terminal instructions** — under 3,900 characters, referencing the Gauntlet block by pointer. Hand over; the user walks away. *(Task 9 — done 2026-08-12: CONTROL/LAUNCH.md.)*

## Build-side (the loops consume these; statuses live in the ledger)
- [ ] WI-01…WI-21 — see the live ledger (document 6) for statuses. The build reads the ledger, not this file.

## Open items that may need a human
- [ ] **WI-18 TrueTone runtime key** — a user-supplied OpenAI-compatible key stored on-device. If the build finds no usable key, this is the ask-the-user fallback at build time (never a silent skip).
- [ ] **Gate 3 critic vision-proof** — send the probe screenshot to the exact judge alias before the first visual verdict (current state §5). If it cannot describe the probe, route or record BLOCKED — never judge unseen screenshots.
